Invisible Cloak (Blue Cloak Version)

This project creates a Harry Potter–style invisible cloak effect using OpenCV.
The camera detects blue-colored cloth and replaces it with the captured background, making the cloth appear invisible.

    Features

Real-time background replacement

Detects cloak based on color masking

Uses HSV color filtering for accurate detection

Works with any blue cloth

Window closes using X (cross button) or Q

     Requirements

Install dependencies:

pip install -r requirements.txt
